name: Test Terraform modules

on:
  pull_request:
    branches:
      - develop
      - main
  push:
    branches:
      - develop
      - main

jobs:
  test:
    name: test all flows
    runs-on: ubuntu-24.04
    strategy:
      max-parallel: 5
      matrix:
        testdir:
          - github
          - gitlab
          - bitbucket
          - azure
          - aws
    steps:
      - name: Checkout repo
        uses: actions/checkout@v6
      - uses: opentofu/setup-opentofu@847eaa4afeb791b06daa46e8eafa8b1b68d7cfb4 # v2.0.1
      - run: pwd
      - name: Fmt check
        working-directory: tests/${{ matrix.testdir }}
        run: tofu fmt-check
        continue-on-error: true
      - name: OpenTofu init
        if: always()
        working-directory: tests/${{ matrix.testdir }}
        run: tofu init -input=false
      - name: Validating
        working-directory: tests/${{ matrix.testdir }}
        run: tofu validate
